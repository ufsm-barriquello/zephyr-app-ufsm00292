# 🎓 Conclusão e Lições Aprendidas

Este projeto foi uma jornada de transição entre a programação de microcontroladores tradicional (*bare-metal*) e o desenvolvimento moderno com Sistemas Operacionais de Tempo Real (RTOS). Abaixo, detalho os principais aprendizados técnicos e metodológicos adquiridos.

## 🧠 Aprendizados Técnicos Consolidados

### Sobre o Hardware (SAMD21)
* **Bare-metal vs. RTOS:** A comparação prática entre controlar GPIOs/Timers manualmente versus delegar isso ao Kernel do sistema operacional.
* **Abstração:** Compreensão de como o hardware físico é abstraído através de drivers, permitindo portabilidade de código.

### Sobre o Ecossistema Zephyr
* **Fluxo de Trabalho:** Domínio dos comandos `west` (build, flash, pristine) como centro do desenvolvimento.
* **Estrutura de Configuração:** Entendimento da "tríade" de configuração do Zephyr:
    1.  `CMakeLists.txt` (Build system)
    2.  `prj.conf` / `Kconfig` (Features do OS)
    3.  `Device Tree` (Descrição de Hardware)

### Sobre Bluetooth Low Energy (BLE)
* **Stack vs. Módulo:** BLE não é apenas hardware. É necessário habilitar a *stack* de software correta e garantir que o `prj.conf` converse com o módulo físico (BTLC1000).
* **Serviços GATT:** Noção prática de como criar serviços e perfis para troca de dados.

---

## 📂 Organização e Metodologia (GitHub)

O uso do GitHub foi além do simples backup de código. Ele serviu como um **Diário de Bordo Técnico**.

* **Documentação Viva:** O registro de falhas, correções e prints de testes (LEDs piscando, logs de build) criou um histórico de evolução claro.
* **Reprodutibilidade:** A organização em pastas por etapas facilita que o *setup* seja replicado no futuro ou compartilhado com colegas sem a necessidade de "reinventar a roda".

---

## ✅ Síntese: O Checklist de Debug

Para finalizar, consolidei um **Checklist de Sobrevivência** para evitar horas perdidas em erros simples. Antes de buscar erros complexos no código, verifico:

1.  **Ambiente:** O PATH está correto? As versões de Python, CMake e Toolchain são compatíveis?
2.  **Estrutura:** Estou rodando o `west build` na raiz correta? O parâmetro `-s` aponta para a aplicação certa?
3.  **Device Tree:** O board `samd21_xpro` está selecionado? Os *overlays* do BTLC1000 usam os nomes de nós (labels) corretos?
4.  **Análise de Logs:**
    * Ignorar o final do log. Subir a tela e procurar o **primeiro** `error:`.
    * Verificar se o erro é recorrente ou pontual.

> *"Seguir este checklist antes de desesperar economiza muito tempo."*

---
